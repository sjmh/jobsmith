# Master Work History — <Your Name>

_Source of truth for generating tailored résumés. This file is an exhaustive
bullet bank to pull from — **never send it directly** as an application. The
tailor-resume skill selects bullets from here **verbatim**; it never rewrites or
invents them, so the quality of every tailored CV is capped by how good these
bullets are._

---

## How to write bullets

Write each bullet **accomplishment-first**: lead with the result or capability
unlocked, include a real metric wherever one honestly exists, then the method.
One tight line per accomplishment so it can be pulled verbatim without editing.
No invented numbers — a bullet without a metric leads with the accomplishment.

**Optional tags.** If you work across distinct specialties, you can prefix bullets
with a `` `[tag]` `` marker (e.g. `` `[Backend]` ``, `` `[Design]` ``) to remember
which roles a bullet suits. The skill strips tags when placing a bullet, and the
verbatim gate ignores them — they're just notes to yourself. Skip them entirely
if you don't need them.

**Optional signature marker.** Put a `+` *inside* a tag (e.g. `` `[Backend +]` ``,
`` `[Backend · Game +]` ``) to flag a crown-jewel accomplishment you want to
appear on *every* tailored CV, even when the role's specialty is a bit off. The
tailor skill treats these as a signature pool and always carries 1–2 of them —
you don't manage that here, the skill does. The marker and its floor/ceiling are
configurable in `resume.config.json` (`signature.marker` / `min` / `max`); it is
stripped along with the tag when the bullet is placed. Leave it off if you don't
need it.

**Conventions to record here** (edit to taste):
- Target level(s):
- Anything intentionally omitted (e.g. unrelated degree):
- Confidentiality / NDA phrasing rules:

---

## Skills

Group your skills into categories the way you'd want them to appear. The skill
selects the JD-relevant subset from this list — it never invents a skill.

**Category A:** skill, skill, skill
**Category B:** skill, skill, skill

---

## Experience

### Company Name | Location | 2021 – Present
**Your Title**
- Accomplishment-first bullet with a real metric and the method that achieved it.
- Another distinct accomplishment — verbatim-ready, one line.
- `[optional-tag]` A bullet you'd only use for a certain kind of role.

### Previous Company | Location | 2018 – 2021
**Your Title**
- ...
- ...

---

## Personal Projects

- **Project Name:** one-line verbatim description of what it is and what you built.
- **Another Project:** ...
